/**
 * author: jaguarcode
 * date:
**/
#include <bits/stdc++.h>
using namespace std;
int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	
	int n;
	cin >> n;
	vector<int> a, p;
	a.resize(n);
	p.resize(n);
	
	for(int i = 0; i < n; i++) cin >> a[i] >> p[i];
	
	int sum = a[0] * p[i];
	for(int i = 1; i < n; i++) {
		
	}
	
	return 0;
}
